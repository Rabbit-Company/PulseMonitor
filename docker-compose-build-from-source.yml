services:
  pulsemonitor:
    build: .
    container_name: pulsemonitor
    volumes:
      - ./config.toml:/config.toml
    restart: unless-stopped
    ulimits:
      nproc: 65535
      nofile:
        soft: 65535
        hard: 65535
